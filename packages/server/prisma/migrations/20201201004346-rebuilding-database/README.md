# Migration `20201201004346-rebuilding-database`

This migration has been generated by SigmaGF at 11/30/2020, 9:43:46 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."clients" (
"id" text   NOT NULL ,
"name" text   NOT NULL ,
"document" text   NOT NULL ,
"group" text   ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."vehicles" (
"id" text   NOT NULL ,
"plate" text   NOT NULL ,
"renavam" text   NOT NULL ,
"brandModel" text   NOT NULL ,
"type" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."receipts" (
"clientId" text   NOT NULL ,
"vehicleId" text   NOT NULL ,
"details" text   ,
"status" text   NOT NULL ,
"issuedOn" timestamp(3)   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("clientId","vehicleId")
)

CREATE UNIQUE INDEX "clients.document_unique" ON "public"."clients"("document")

CREATE UNIQUE INDEX "vehicles.plate_unique" ON "public"."vehicles"("plate")

CREATE UNIQUE INDEX "vehicles.renavam_unique" ON "public"."vehicles"("renavam")

ALTER TABLE "public"."receipts" ADD FOREIGN KEY ("clientId")REFERENCES "public"."clients"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."receipts" ADD FOREIGN KEY ("vehicleId")REFERENCES "public"."vehicles"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201201001623-b..20201201004346-rebuilding-database
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model User {
   id              String    @id @default(uuid())
@@ -50,14 +50,13 @@
   @@map("vehicles")
 }
 model Receipt {
-  id        String    @unique @default(uuid())
   clientId  String
   vehicleId String
   details   String?
   status    String
-  issuedOn  DateTime?
+  issuedOn  DateTime
   createdAt DateTime  @default(now())
   updatedAt DateTime  @default(now()) @updatedAt
   client    Client    @relation("receipt-client", fields: [clientId], references: [id])
```


