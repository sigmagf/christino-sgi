# Migration `20201210013440-crv-to-crv`

This migration has been generated by SigmaGF at 12/9/2020, 10:34:40 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201210013342-receipts-to-crv..20201210013440-crv-to-crv
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model User {
   id        String   @id @default(uuid())
@@ -25,9 +25,9 @@
   group     String?
   createdAt DateTime  @default(now())
   updatedAt DateTime  @default(now()) @updatedAt
-  Receipt   CRV[] @relation("receipt-client")
+  Receipt   Crv[] @relation("receipt-client")
   @@map("clients")
 }
@@ -39,14 +39,14 @@
   type       String
   createdAt  DateTime  @default(now())
   updatedAt  DateTime  @default(now()) @updatedAt
-  Receipt    CRV[] @relation("receipt-vehicle")
+  Receipt    Crv[] @relation("receipt-vehicle")
   @@map("vehicles")
 }
-model CRV {
+model Crv {
   clientId  String
   vehicleId String
   details   String?
   status    Int       @default(1)
```


