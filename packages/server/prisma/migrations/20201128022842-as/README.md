# Migration `20201128022842-as`

This migration has been generated by SigmaGF at 11/27/2020, 11:28:42 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER INDEX "public"."clientes.documento_unique" RENAME TO "clients.document_unique"

ALTER INDEX "public"."veiculos.placa_unique" RENAME TO "vehicles.plate_unique"

ALTER INDEX "public"."veiculos.renavam_unique" RENAME TO "vehicles.renavam_unique"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201128022114-aa..20201128022842-as
--- datamodel.dml
+++ datamodel.dml
@@ -3,12 +3,12 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
-model Usuario {
+model User {
   id               String     @id @default(uuid())
   name             String
   email            String
   password         String
@@ -22,45 +22,45 @@
   createdAt        DateTime   @default(now())
   updatedAt        DateTime   @default(now()) @updatedAt
-  @@map("usuarios")
+  @@map("users")
 }
-model Cliente {
+model Client {
   id         String    @id @default(uuid())
-  nome       String
-  documento  String    @unique
-  grupo      String?
+  name       String
+  document   String    @unique
+  group      String?
   createdAt  DateTime  @default(now())
   updatedAt  DateTime  @default(now()) @updatedAt
-  @@map("clientes")
+  @@map("clients")
 }
-model Veiculo {
-  id           String     @id @default(uuid())
-  placa        String     @unique
-  renavam      String     @unique
-  marcaModelo  String
-  tipo         String
-  createdAt    DateTime   @default(now())
-  updatedAt    DateTime   @default(now()) @updatedAt
+model Vehicle {
+  id          String     @id @default(uuid())
+  plate       String     @unique
+  renavam     String     @unique
+  brandModel  String
+  type        String
+  createdAt   DateTime   @default(now())
+  updatedAt   DateTime   @default(now()) @updatedAt
-  @@map("veiculos")
+  @@map("vehicles")
 }
-model Recibo {
-  id           String     @id @default(uuid())
-  clienteId    String
-  veiculoId    String
-  detalhes     String?
-  emitidoEm    DateTime?
-  situacao     String
-  createdAt    DateTime   @default(now())
-  updatedAt    DateTime   @default(now()) @updatedAt
+model Receipt {
+  id         String     @id @default(uuid())
+  clientId   String
+  vehicleId  String
+  details    String?
+  issuedOn   DateTime?
+  status     String
+  createdAt  DateTime   @default(now())
+  updatedAt  DateTime   @default(now()) @updatedAt
-  cliente     Cliente  @relation("recibo-cliente", fields: [clienteId], references: [id])
-  veiculo     Veiculo  @relation("recibo-veiculo", fields: [veiculoId], references: [id])
+  client     Client   @relation("receipt-client", fields: [clientId], references: [id])
+  vehicle    Vehicle  @relation("receipt-vehicle", fields: [vehicleId], references: [id])
-  @@map("recibos")
+  @@map("receipts")
 }
```


