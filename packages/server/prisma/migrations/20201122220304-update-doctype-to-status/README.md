# Migration `20201122220304-update-doctype-to-status`

This migration has been generated by SigmaGF at 11/22/2020, 7:03:04 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201122215935-corrigindo-o-vehicle..20201122220304-update-doctype-to-status
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Client {
   id         String    @id @default(uuid())
@@ -33,12 +33,11 @@
 model Crv {
   id         String    @id @default(uuid())
   clientId   String
   vehicleId  String
-  docType    String
   issuedOn   DateTime?
   details    String?
-  status     Boolean
+  status     String
   createdAt  DateTime  @default(now())
   updatedAt  DateTime  @default(now()) @updatedAt
   client     Client  @relation("crv-client", fields: [clientId], references: [id])
```


