# Migration `20201128021047-add-user`

This migration has been generated by SigmaGF at 11/27/2020, 11:10:47 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Usuario" (
"id" text   NOT NULL ,
"nome" text   NOT NULL ,
"email" text   NOT NULL ,
"password" text   NOT NULL ,
"newEmail" text   ,
"newEmailToken" text   ,
"newEmailExpires" text   ,
"pwdRstToken" text   ,
"pwdRstExpires" timestamp(3)   ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201128010522-d..20201128021047-add-user
--- datamodel.dml
+++ datamodel.dml
@@ -3,11 +3,28 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
+model Usuario {
+  id               String     @id @default(uuid())
+  nome             String
+  email            String
+  password         String
+  
+  newEmail         String?
+  newEmailToken    String?
+  newEmailExpires  String?
+
+  pwdRstToken      String?
+  pwdRstExpires    DateTime?
+
+  createdAt        DateTime   @default(now())
+  updatedAt        DateTime   @default(now()) @updatedAt
+}
+
 model Cliente {
   id         String    @id @default(uuid())
   nome       String
   documento  String    @unique
```


