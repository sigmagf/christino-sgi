# Migration `20201128021602-fix-users`

This migration has been generated by SigmaGF at 11/27/2020, 11:16:02 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."usuarios" (
"id" text   NOT NULL ,
"nome" text   NOT NULL ,
"email" text   NOT NULL ,
"senha" text   NOT NULL ,
"newEmail" text   ,
"newEmailToken" text   ,
"newEmailExpires" text   ,
"pwdRstToken" text   ,
"pwdRstExpires" timestamp(3)   ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)

DROP TABLE "public"."Usuario"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201128021047-add-user..20201128021602-fix-users
--- datamodel.dml
+++ datamodel.dml
@@ -3,17 +3,17 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Usuario {
   id               String     @id @default(uuid())
   nome             String
   email            String
-  password         String
-  
+  senha            String
+
   newEmail         String?
   newEmailToken    String?
   newEmailExpires  String?
@@ -21,8 +21,10 @@
   pwdRstExpires    DateTime?
   createdAt        DateTime   @default(now())
   updatedAt        DateTime   @default(now()) @updatedAt
+
+  @@map("usuarios")
 }
 model Cliente {
   id         String    @id @default(uuid())
```


