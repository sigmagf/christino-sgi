# Migration `20201120230144-update`

This migration has been generated by SigmaGF at 11/20/2020, 8:01:44 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP INDEX "public"."veichles.plate_unique"

DROP INDEX "public"."veichles.renavam_unique"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201120224546-reload..20201120230144-update
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Client {
   id         String    @id @default(uuid())
@@ -19,10 +19,10 @@
 }
 model Veichle {
   id          String    @id @default(uuid())
-  plate       String    @unique
-  renavam     String    @unique
+  plate       String
+  renavam     String
   brandModel  String
   type        String
   createdAt   DateTime  @default(now())
   updatedAt   DateTime  @default(now()) @updatedAt
```


