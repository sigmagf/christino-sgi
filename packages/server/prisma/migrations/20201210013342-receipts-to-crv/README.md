# Migration `20201210013342-receipts-to-crv`

This migration has been generated by SigmaGF at 12/9/2020, 10:33:42 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."receipts" DROP CONSTRAINT "newReceipt_clientId_fkey"

ALTER TABLE "public"."receipts" DROP CONSTRAINT "newReceipt_vehicleId_fkey"

CREATE TABLE "public"."crvs" (
"clientId" text   NOT NULL ,
"vehicleId" text   NOT NULL ,
"details" text   ,
"status" integer   NOT NULL DEFAULT 1,
"issuedOn" timestamp(3)   ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("clientId","vehicleId")
)

ALTER TABLE "public"."crvs" ADD FOREIGN KEY ("clientId")REFERENCES "public"."clients"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."crvs" ADD FOREIGN KEY ("vehicleId")REFERENCES "public"."vehicles"("id") ON DELETE CASCADE ON UPDATE CASCADE

DROP TABLE "public"."receipts"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201207231335-remove-vehicle-from-works..20201210013342-receipts-to-crv
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model User {
   id        String   @id @default(uuid())
@@ -25,9 +25,9 @@
   group     String?
   createdAt DateTime  @default(now())
   updatedAt DateTime  @default(now()) @updatedAt
-  Receipt   Receipt[] @relation("receipt-client")
+  Receipt   CRV[] @relation("receipt-client")
   @@map("clients")
 }
@@ -39,14 +39,14 @@
   type       String
   createdAt  DateTime  @default(now())
   updatedAt  DateTime  @default(now()) @updatedAt
-  Receipt    Receipt[] @relation("receipt-vehicle")
+  Receipt    CRV[] @relation("receipt-vehicle")
   @@map("vehicles")
 }
-model Receipt {
+model CRV {
   clientId  String
   vehicleId String
   details   String?
   status    Int       @default(1)
@@ -57,9 +57,9 @@
   client    Client    @relation("receipt-client", fields: [clientId], references: [id])
   vehicle   Vehicle   @relation("receipt-vehicle", fields: [vehicleId], references: [id])
   @@id([clientId, vehicleId])
-  @@map("receipts")
+  @@map("crvs")
 }
 model Work {
   id          String      @id @default(uuid())
```


