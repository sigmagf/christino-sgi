# Migration `20201201210556-re-receipts-2`

This migration has been generated by SigmaGF at 12/1/2020, 6:05:56 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."newReceipt" (
"clientId" text   NOT NULL ,
"vehicleId" text   NOT NULL ,
"details" text   ,
"status" integer   NOT NULL DEFAULT 1,
"issuedOn" timestamp(3)   ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("clientId","vehicleId")
)

ALTER TABLE "public"."newReceipt" ADD FOREIGN KEY ("clientId")REFERENCES "public"."clients"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."newReceipt" ADD FOREIGN KEY ("vehicleId")REFERENCES "public"."vehicles"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201201210521-fix-receipts..20201201210556-re-receipts-2
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model User {
   id        String   @id @default(uuid())
@@ -73,9 +73,8 @@
   client    Client    @relation("receipt-client", fields: [clientId], references: [id])
   vehicle   Vehicle   @relation("receipt-vehicle", fields: [vehicleId], references: [id])
   @@id([clientId, vehicleId])
-  @@map("receipts")
 }
 model Work {
   id          String      @id @default(uuid())
```


