# Migration `20201201232220-add-detran-id-in-works`

This migration has been generated by SigmaGF at 12/1/2020, 8:22:20 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."works" ADD COLUMN "detranId" text   NOT NULL 
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201201210951-receipts-fixed..20201201232220-add-detran-id-in-works
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model User {
   id        String   @id @default(uuid())
@@ -64,8 +64,9 @@
 model Work {
   id          String      @id @default(uuid())
   clientId    String
   vehicleId   String
+  detranId    String
   description String
   status      Int         @default(1)
   createdAt   DateTime    @default(now())
   updatedAt   DateTime    @default(now()) @updatedAt
```


