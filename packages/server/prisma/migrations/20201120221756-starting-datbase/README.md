# Migration `20201120221756-starting-datbase`

This migration has been generated by SigmaGF at 11/20/2020, 7:17:56 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."clients" (
"id" text   NOT NULL ,
"name" text   NOT NULL ,
"document" text   NOT NULL ,
"group" text   ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."veichles" (
"id" text   NOT NULL ,
"plate" text   NOT NULL ,
"renavam" text   NOT NULL ,
"brandModel" text   NOT NULL ,
"type" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."crvs" (
"id" text   NOT NULL ,
"clientId" text   NOT NULL ,
"veichleId" text   NOT NULL ,
"docType" text   NOT NULL ,
"issuedOn" timestamp(3)   NOT NULL ,
"details" text   ,
"status" boolean   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "clients.document_unique" ON "public"."clients"("document")

CREATE UNIQUE INDEX "veichles.plate_unique" ON "public"."veichles"("plate")

CREATE UNIQUE INDEX "veichles.renavam_unique" ON "public"."veichles"("renavam")

ALTER TABLE "public"."crvs" ADD FOREIGN KEY ("clientId")REFERENCES "public"."clients"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."crvs" ADD FOREIGN KEY ("veichleId")REFERENCES "public"."veichles"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201120221756-starting-datbase
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,48 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+model Client {
+  id         String    @id @default(uuid())
+  name       String
+  document   String    @unique
+  group      String?
+  createdAt  DateTime  @default(now())
+  updatedAt  DateTime  @default(now()) @updatedAt
+
+  @@map("clients")
+}
+
+model Veichle {
+  id          String    @id @default(uuid())
+  plate       String    @unique
+  renavam     String    @unique
+  brandModel  String
+  type        String
+  createdAt   DateTime  @default(now())
+  updatedAt   DateTime  @default(now()) @updatedAt
+
+  @@map("veichles")
+}
+
+model crv {
+  id         String    @id @default(uuid())
+  clientId   String
+  veichleId  String
+  docType    String
+  issuedOn   DateTime
+  details    String?
+  status     Boolean
+  createdAt  DateTime  @default(now())
+  updatedAt  DateTime  @default(now()) @updatedAt
+
+  client     Client  @relation("crv-client", fields: [clientId], references: [id])
+  veichle    Veichle @relation("crv-veichle", fields: [veichleId], references: [id])
+  
+  @@map("crvs")
+}
```


